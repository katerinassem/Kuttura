import React from 'react';
import { Container } from '@material-ui/core';

function Spinner() {
  return (
    <Container maxWidth="sm">Please wait until data is loaded</Container>
  );
}

export default Spinner;
